node_modules/
worker/node_modules/
.wrangler/
dist/
*.env
.env.local
.dev.vars
test-results/
playwright-report/
android/app/google-services.json
android/upload.jks

# Build artifacts — CI uploads these as workflow artifacts / release assets;
# committing them (23MB aab, 485k-line mapping) bloats the repo forever.
*.aab
mapping.txt
mapping.txt.gz

# Hermes agent working files (plans, scratch)
.hermes/
